# Changelog — one line per push (newest first)
- 2026-09-24 · Drift guards + staged fixes (branch fix/edge-drift-reconcile-and-guards; nothing deployed, nothing applied, no page changed). Edge functions: quo-inbound-webhook rebuilt on live v13 (v12/v13 had shipped after the Sep 22 plan) and lead-notify on live v5, each from two independent byte-identical copies of the deployed source, plus only the named additions (a completed call or an outbound text moves a lead out of "new"; campaign/landing-page rows + a Text button in the new-lead alert); every live fix kept (real 10-digit number before any lookup, one row per call, LeadFlow Pro line skipped, duplicate-delivery claim, sender from app_secrets). Sweep of the other eight: four identical to live; kajabi-webhook, meta-lead-webhook (retired 410 stub) and enroll-welcome now mirror live; buy-product diverged both ways and is marked do-not-deploy. NEW supabase/functions/DEPLOYED.json (all 45 live functions) + check:edge-drift (fails on a mirror that no longer matches its record, a missing/stale DO-NOT-DEPLOY block, an unrecorded function; --live catches deploys made outside git) + check:edge-behavior (runs the real lead-path code on fictional data; fails on the old stale copies). Migrations: the seven applied-but-unfiled migrations recovered byte-identical from schema_migrations; the Quo claim table, two unique indexes and two cron jobs that were in no migration reconstructed from the catalog; NEW db/migrations/MANIFEST.json (every file's applied/staged provenance, all 148 live migrations, probe evidence for the nine applied without a history row) + check:migrations. Staged SQL: lead-alert retry now states its prerequisite (live lead-notify already accepts the header), no longer anon-callable, cannot re-send the last five hours of alerts on first run, 20 s timeout, verbatim rollback; $397 promo marked Amanda-only pricing; NEW db/pending/20260924_close_anon_campaign_metrics.sql (email-campaign metric views off the anon key, three trigger functions and eight backup tables' leftover grants revoked; the LeadFlow Pro scoreboard and other public RPCs untouched). docs/edge-functions.md runbook; lead-email runbook ship order. npm test (17 validators) + test:e2e clean.
- 2026-09-24 · /classes showed every class one day early (the Monday, Oct 5 class read "Sun, Oct 4"; Sep 29 read "Mon, Sep 28"; Nov 9 read "Sun, Nov 8"): its date formatter parsed the cohort's calendar date as midnight UTC, which is the evening before in Central time. It now builds the date from its year/month/day so every visitor sees the right day; /graduates had the same bug in its month labels and got the same fix. /calendar, the homepage countdown, /enroll, the nav banner and the funnel pages already parsed dates correctly and are unchanged. NEW scripts/check-date-display.mjs (in npm test) fails the build if a public page formats a *_date field with new Date(x) and runs the /classes formatter in America/Chicago against the real cohort dates. Amanda approved the fix Sep 24. npm test clean.
- 2026-09-22 · Speed-to-lead and attribution live (merges the Sep 19–20 work that had been sitting unmerged): every public form — /apply, /contact, /tour, /waitlist, /study-guide, /employers/request-graduate, /sponsor-a-student, /labor-day, /classes and the practice-exam unlock — now runs through one shared module (assets/pda-lead.js) with an honest failure path (the visitor's answers stay in the form, the button re-enables, and an error with the phone number appears — never a false "you're in"), a local stash retried on the next page load, and first/last-touch UTM + landing page folded into the lead; NEW api/lead.js (server insert with an email fallback and an honest 502 rather than a silent drop), NEW api/go.js + data/go-links.mjs (20 /go short links, OG-complete). The two edge functions this work touches were deliberately NOT deployed: both repo mirrors turned out to be STALE against live, in opposite directions — deployed quo-inbound-webhook v11 (Sep 22) requires a real 10-digit number before touching a lead, keeps one row per call, and drops LeadFlow Pro line traffic, none of which is in the repo; deployed lead-notify v5 reads its sender identity from app_secrets because a hardcoded sender drew a Resend 403. Shipping either repo copy would have undone live fixes and, for lead-notify, silently stopped Amanda's new-lead alerts. Both files now carry a DO-NOT-DEPLOY header naming what each side is missing. db/migrations/20260920_reconcile_contacted_leads.sql applied (330 historically mislabeled leads moved from "new" to "contacted" on real evidence, so /admin/kpi stops overstating uncontacted leads). Security: revoked EXECUTE from PUBLIC on the ten SECURITY DEFINER CRM functions the Sep 8 migrations left reachable by the anon key (the original revoke named only `anon`, which Postgres's default PUBLIC grant defeated) and fixed the NULL-guard fall-through in release_lead. The six /go funnel share previews now state the $3,500 plan total wherever "$500 down" appears, matching the on-page copy. scripts/e2e-smoke.mjs corrected (it never ticked the waitlist consent box, and it asserted the study-guide form hides when the page hides the wrapping gate and deliberately keeps the form up on failure). npm test (14 validators) + test:e2e clean. Still staged, not applied: the $397 online promo and the lead-notify retry net.
- 2026-09-20 · Blog: two posts from the Airtable queue — "Why Amanda Started Premier Dental Academy" (founder story built only from approved /about copy: graduated this program, worked chairside in East Texas, bought the school; the three stated values as a tabs widget; links /about, /classes, /tour, /tools/practice-pro, /tools/chairside, /tools/tuition-planner) and "The Infection-Control Mistakes New Dental Assistants Make" (five habit errors and the replacement habit for each, click-to-reveal cards; principles only — no cycle times, temperatures or citation numbers, every specific deferred to the office protocol, the manufacturer and tsbde.texas.gov). No hard-coded prices in either (links to /enroll and /tools/tuition-planner instead), cohort dates from Supabase (Sept 29 / Oct 5 / Oct 20 / Nov 9), www canonicals, branded 2400×1260 share cards rendered on scripts/generate-og.mjs. blog.html cards + sitemap entries added.
- 2026-09-19 · Facebook-traffic conversion fixes, increment 1 (Amanda's Sep 17 brief; inventory in docs/CONVERSION-AUDIT-2026-09-17.md): assets/site-facts.js is now the single source for prices (in-person $3,000 PIF / $3,500 plan = $500 + $3,000; online regular $997 with a one-switch $397 promo that stays OFF until Amanda approves the checkout change), an approved-claims register (placement rate, graduate count, "70% no experience", salary/payback, superlatives, interview timing all unapproved → hidden or neutral copy), retired testimonials, and a retired-facts list (1405 McCann, 230-6444, $1,997, $200 down, "Premiere"). Homepage: countdown no longer types "Sep 29" (plain REST fetch, America/Chicago, 8s timeout, honest "being scheduled / call us" fallbacks, never blank), "students graduated" and "placement rate" tiles dropped (public_stats_overrides graduates_extra=394 / placement_rate_pct=85 no longer read), pricing block states both in-person numbers, "regular price price" typo + duplicate struck $997 fixed, "only RDA program"/"$42k+"/"pays back in 4–8 weeks"/"week 10"/"offers within 2 weeks"/"70%" copy replaced, JSON-LD FAQ + meta/og descriptions matched. /classes and /calendar: Supabase SDK failure or query error now shows a call-us card instead of "loading…" forever or a false "no classes"; upcoming excludes classes whose date has passed; calendar prices read site-facts; typed "8 seats" copy → live seat counts; reserve form shows an inline error and keeps entries when the save fails. /enroll: JSON-LD says TSBDE (not TDLR) and awards the PDA Certificate of Completion (not the state RDA credential); "8 seats" proof tile → "$3,500 on a plan ($500 down)". Ask Premier chatbot: online is self-paced (not "live video"), no salary/offer-timing/70% claims. Nine pages' og/twitter "lowest tuition in East Texas" → neutral. Funnel pages (/go/*) and tools state the $3,500 plan total wherever "$500 down" appears. NEW scripts/check-claims.mjs (in npm test) fails the build on any retired claim, old contact, or typed countdown date; check:pricing now asserts api/enroll.js + enroll.html integers equal site-facts and that "$500 down" never appears without "$3,500". npm test clean.
- 2026-09-17 · September class post retitled around the open date: "September Dental Assistant Class in Longview: Sept 29 Start (Plus October Dates)" (same URL). Intro, stat tile, description, share metadata and blog card now say the Sept 14 class is in session and Sept 29 / Oct 5 / Oct 20 are open; card re-rendered.
- 2026-09-17 · Stale class dates refreshed: the September 14 class has started, so 96 blog posts + the homepage fallback still calling it an upcoming start now list the next confirmed dates (Sept 29 T/Th, Oct 5 MWF, Oct 20 T/Th, Nov 9 MWF, Nov 17 T/Th, per site-facts/cohorts). Tour-CTA date grids rebuilt in date order; prose swapped date-for-date with no new claims; the dated August/September class-announcement posts and the expired Labor Day copy left as historical. check:dates green again; npm test clean.
- 2026-09-17 · Tool/landing-page share cards moved onto the same generator (`npm run og`, PAGES table in scripts/generate-og.mjs → assets/og/page-<key>.jpg): /salary, /tools/practice-exam + /go/rda-practice-exam, /skills-lab, /enroll, /tools/share-your-win (old card showed an invented student name — gone), /tools/tuition-planner, /sponsor-a-student, /employers/request-graduate. Headlines are each page's own title wording; chips repeat only site-facts pricing/plan facts or the page's own copy; the unverified "no placement fee" line was dropped from the request-graduate image alt. Nine old og-*.png/jpg files deleted (incl. the unreferenced salary card with a stock-style person). og:image width/height/type set on all nine pages. npm test clean.
- 2026-09-09 · Social-preview (OG) cards redesigned: every blog post (242) and the site-wide cover now share one branded 2400×1260 card (Fraunces headline in the post's own og:title, Inter, navy→teal palette, logo mark, per-category accent + icon, the anatomical arch from assets/tooth-shapes.js as line art, TWC #S5316, Longview + phone from site-facts). New scripts/generate-og.mjs (`npm run og`, Playwright, fonts vendored in scripts/og-fonts) replaces the flat teal PNGs; 107 posts that fell back to the generic cover now get their own card; og:image:width/height/type added to every post; build_blog_batch.py renders the card for new posts; all og-cover.png references → og-cover.jpg. npm test clean.
- 2026-09-07 · Admin menu + "money in open seats" (Ryan: menu looked weird on desktop, worse on phones): assets/admin-nav.js desktop is now two clean rows — four group tabs (Daily/People/Teaching/Marketing, each carrying the summed waiting-work count) over one row of that group's links, current page's group preselected; below 1280px a single 56px row with the current page named next to the brand and a Menu button opening a grouped panel (4 columns tablet / 2 phone, 44px targets, wrapping labels, Escape/outside-tap closes). /admin/cohorts: fixed the invisible "Students enrolled" card (white text on white), added a Money-waiting panel (open seats in not-yet-started in-person classes × the site-facts $3,000 pay-in-full price, with the $3,500-plan ceiling and running-class seats listed separately, never guessed when the facts file is missing), and per class the lead most likely to fill a seat — scored from stage, grade, ready month vs. start month, MWF/T-Th preference, payment-plan intent, naming the class, freshness and contact gap, each pick shown with plain-English reasons, Call/Text/Email/Open-lead actions, two runners-up, one class per lead; enrolled/lost/not-qualified/online-only/test leads excluded; texting a lead logs related_lead_id. /admin/leads#lead=<id> deep link opens that lead's drawer. docs/NAV-QA.md updated. npm test + 39-check headless suite (desktop/laptop/phone, blocked-leads fallback, fictional data) clean.
- 2026-09-07 · Labor Day final-gate fixes: seat labels now show the REAL open-seat count everywhere (assets/seat-mask.js cap removed; "3 seats left" while 6 were open was invented urgency), the six /go/ funnel pages swap their "$500 down holds your seat" top bar for the live Labor Day line + /labor-day link while the offer clock in site-facts is live (funnel-kit.js, self-reverts at expiry), /enroll tells a plan buyer who switches to an October/November class that the regular $500 down applies and why, homepage promo bar polls for site-facts instead of reading it once, and the purchases trigger no longer opens a false priority-1 "assign a cohort" task when the checkout webhook already enrolled the student in a class (db/migrations/20260907_no_false_assign_cohort_task.sql, applied). npm test + headless Labor Day suite clean.
- 2026-09-07 · Course platform pass (Amanda: charting exercises, English only, layout students want to click through): NEW /tools/charting-drills (five levels on the real anatomical chart: find the tooth, name the surface, chart existing work in blue, chart the treatment plan in red, full-mouth dictation; streaks, badges, celebration, localStorage bests, pp_practice_log scores for signed-in students so the gradebook and dashboard see them; levels 1 and 2 open, 3 to 5 for enrolled students via PDA_ACCESS; phone zoom + swipe, keyboard/buttons fallback, no invented conventions: sealants occlusal only, caries left out); NEW assets/pda-odontogram.js (Practice Pro's chart as a shared library: render/mount/bind); /learn redesigned as a journey (catalog greets by first name with a "continue where you left off" card + per-course progress rings; course page groups weeks under their unit labels with the current week auto-opened, "Next" pill, minutes left; lesson page has a week progress bar, a sticky this-week sidebar, "Week N · Lesson x of y", a done panel that names the next lesson / "Week N done" / certificate with a reduced-motion-safe celebration, tooth_surfaces widget on the real chart, practice links for interactive keys that had no widget, error + retry state); DB (applied, files in db/migrations): Chapter 34 Spanish video removed from both courses, 76 quiz lessons had the stale "being moved over from our previous system" notice replaced by a real intro (question count + pass mark), Chapters 11/12/28/48/50 in both courses now link to the matching drill level with a return path; exit-intent popup suppressed on /learn, /certificate, /paperwork and the drills; nav + tools page + sitemap link the drills. docs/course-video-audit.md lists every lesson video for an English check. npm test + 52 headless checks clean.
- 2026-09-06 · Labor Day 2026 offer + cohort fixes + payment visibility (branch feat/labor-day-offer-2026-09, NOT deployed; runbook docs/labor-day-2026-offer.md): dated $100 seat-reservation offer ($100 + $3,000 = $3,100 plan, Sept 14 / Sept 29) lives once in site-facts (laborDay2026 + offerIsLive helper; check:facts fails a build that is still active past the Sep 7 11:59:59 PM CT deadline); new /labor-day landing page (live countdown in America/Chicago, approved copy verbatim, per-class Square deposit links read from cohorts.deposit_link_url with a call-us state when NULL, lead capture into leads tagged utm_campaign=laborday2026 with localStorage fallback, required §807.173 name/address disclosure, self-expiring "offer ended" panel → /enroll; noindex, not in sitemap); site-wide promo bar (pda-nav.js injectPromoBar, shows on /enroll too) + homepage bar, both a reduced-motion-safe shimmer (not a blink) and both gated on offer-live AND a real deposit link existing; calendar/classes date summaries now read live cohorts (no hardcoded "Sep 14 · Sep 29 · Nov 9 · Nov 17" or static prerender cards), fall-dates blog post no longer counts "six" dates; NEW /admin/payments (owner-only) + read-only api/admin-payments.js Square feed: every payment vs purchases, "in Square but not in our records" gap, "down paid, no balance schedule" flag, failed charges as private action items; Meta pixel id filled in analytics-config.js so PDA.track custom events reach Meta; staged-not-applied SQL in db/pending (October 5 MWF + October 20 T/Th cohorts with the MWF time block left for Amanda; Selena → Sept 29 move + task close); Selena correction email staged as a Gmail draft (not sent). npm test (+ new check:labor-day) and a 55-check headless browser suite clean.
- 2026-09-05 · Second wave (Amanda-approved to go live): Practice Pro tooth chart redrawn as a real anatomical odontogram (assets/tooth-shapes.js — hand-drawn crowns + roots per tooth type in two curved arches, patient's right on the viewer's left, chewing-surface pickers, missing/implant/crown/RCT/abscess/fracture/bridge/diastema states, keyboard + phone friendly, fixes stacked click handlers that re-rendered N× per click); teacher visibility (instructor read policies + admin_student_activity/admin_student_progress RPCs, anon revoked on staff functions); /admin/progress gradebook with per-student lessons/quizzes/questions/skills, student activity drawer (reopen quiz, answer question, verify skill, printable competency record), instructors admitted with a role-aware menu; /dashboard my quizzes + practice-exam history + verified skills; leads CRM: human-only last-contact stamping (trigger + backfill), missed-call callback queue, who-to-call buckets, duplicate chips, enrolled stage, safer SMS composer, night-class text template retired; Skills Lab: reliable attempt sync, 80% credit rule + step timers, rubrics on all 40 skills, DB-backed instructor sign-off, honest progress record, preview gating, touch tray builder. Migrations applied (5). npm test + headless suites clean.
- 2026-09-05 · Admin + student dashboard usability (Amanda-approved to go live): admin nav shows all 16 pages with no side-scroll (grouped wrapped rows on desktop, Menu button + 2-column list on phones, plain-English names, live waiting-work badges); /admin/leads lead profile drawer (reach them, what they want, source/landing page/UTM, full contact history with counts + Quo summaries/transcripts, notes, open tasks) opened from the name in every view; Call/Text/"I handled this" now log to communications with one-tap outcome chips; list rows show intent/source/last outbound contact/touch counts + Never contacted / 7+ days / Hot / Program / Free-tool chips; failed queries render red errors instead of empty states on 8 admin pages; hub cards + counters for every page; approvals confidence badges + quick-review sort + pending count (still one-at-a-time human approval); instructor gate message on /admin/progress; /dashboard real lesson progress from module_progress, certificate link at 100%, Skills Lab status, cohort from cohorts, products from the products table ("Included" only when owned), guest purchases by email, PDAPortal.requireEnrolled gate with visible error/suspended/instructor cards; portal CTAs visible on touch; login no-op Remember-me removed. Migrations applied: communications.related_lead_id index + purchases_self_email RLS policy. 28-scenario headless suite + npm test clean.
- 2026-08-30 · Career Direction Plan v4 — polish + depth + pricing (Ryan review round): ZERO emoji (replaced site-wide on the page by a hand-drawn soft SVG stroke-icon set) and zero em dashes (both now regression-tested); hero art jpg replaced by a pure-CSS ANIMATED app preview (chips highlight + direction bars fill on loop, "it builds, live"); new "real talk" reflection layer (3 voice/type prompts: what would you pick if nobody had an opinion, name the fear w/ 5 tailored honest answers, good-life-at-25) quoted back verbatim in the live panel + plan page; floating Get-it CTA (appears on scroll, hides at checkout); price per left-digit research: $19 launch / $47 regular (products row updated in prod, migration 20260830_career_plan_price_19; quiz CTA + ld+json + all page copy updated; page verified to contain ONLY $19/$47). 29-check suite + npm test clean; product still active=false; $47 must become the real price post-launch or the anchor comes down.
- 2026-08-30 · Career Direction Plan v3 — done-for-you app w/ LIVE plan build (Ryan: no prose walls, no em dashes, plan builds live as they answer, audience-fit soft design): /career-plan owned view is now a step-by-step builder (one question per screen; tap chips, 1-5 taps, type, or TALK via Web Speech mic) with a sticky "Your plan · building live" panel that fills slot-by-slot in real time (live direction bars during the 8-tap ranker, totals computing as costs are typed, shadow message preview, one-pager status) + "✨ Added to your plan" toast; ends in a generated plan page (greets by name, computed trade/route totals + cheaper-by verdict, written shadow message w/ copy, tappable test week, tap-and-go resource buttons, generated parent one-pager addressed to their chosen person) with full access from the first tap, resume-where-you-left-off, switch-direction, start-over; complete visual redesign to soft rose/cream/sage/lavender palette w/ sparkle accents for the young-women audience; zero em dashes page-wide; sales page repitched around watching it build. 31-check headless suite + npm test clean; checkout unchanged; product still active=false.
- 2026-08-30 · Career Direction Plan v2 — full interactive buildout (Ryan: "$20 needs clickables/workables"): /career-plan workbook now has a Direction Fit Ranker (8 statements → live-ranking bars + leading-plan verdict), tap-to-check 7-day test weeks with per-plan counters, worksheets as real inputs that autosave to the buyer's device (localStorage, restores on reload) + overall %-worked progress bar, per-path power tools (true-cost trade comparator, shadowing-script builder w/ copy button, three-route degree cost comparator w/ borrow bars, commitment comparator + 14-day cooling-off date calc), gradient plan banners, sales page gains quiz hero art + live demo checklist + tool-led copy; "My Plan" compiles the buyer's own ranker result/answers/next actions into a printable personal plan. 31-check headless suite (incl. persistence across reload, calculator math, zero page errors) + npm test clean; checkout unchanged; product still active=false.
- 2026-08-30 · Career Direction Plan ($20 product, Amanda-approved, NOT YET ACTIVE): /career-plan sales page + full in-page workbook (all four quiz directions — hands-on credential, people-facing skill, academic, structured — each w/ honest write-up, 7-day test-it-cheap plan, real-links-only resource list, decision worksheet, parent one-pager) gated on new profiles.career_plan flag, checkout via buy-product (study-pack pattern); products row career_plan $20/reg $29 active=false (migration 20260830, applied) so nothing is buyable until Amanda's go-ahead; quiz results add paid-instant option alongside the free Ask-Amanda path (quiz_plan_click); sitemap +/career-plan. npm test clean + 19-check headless smoke (loading/error/success/owned states, fictional data only).
- 2026-08-26 · Employer request OG preview: added a dedicated 1200×630 `/assets/og-request-graduate.jpg` social image for `/employers/request-graduate`; page-specific Open Graph and X/Twitter title, description, image dimensions, media type, canonical URL, and accessible alt metadata.
- 2026-08-16 · Online strike-price campaign (Amanda-approved): NEW /online statewide landing page (Course JSON-LD w/ priceValidUntil, real 13-module/119-lesson stats, live countdown, campaign OG + 5 social graphics in assets/social/); date-gated $397→$997 auto-flip at Sat Aug 22 00:00 CT (July-1 pattern) across enroll/courses/index/calendar/site-facts/api-enroll — charge side flips via api/enroll.js ONLINE_CUTOVER_MS + a pg_cron products.online_program price_cents flip (job pda-online-price-flip, self-unscheduling); online bundle shipped (buy-product v6 also grants exam_pro/study_pack/exam_prep; missing profiles.online_program column added — was logging granted=false on every online sale); "East Texas's only" removed (index JSON-LD + hero, enroll, guide, pda-seo default); index "attended live from home" → self-paced; deadline phrasing on all $397 money surfaces; unverified "weekly live Q&A" dropped from products blurb; sitemap +/online. npm test clean.
- 2026-08-08 · Directory round 2: 28 city hub pages (/directory/<city> — office grid, nearby-city links, CollectionPage+Breadcrumb JSON-LD, training + claim CTAs; sitemap'd at 0.7), verified office hours + contact email rendered on profiles (3 Longview offices verified from their own sites 7/31, per-office source + date shown), 4-level breadcrumbs (… → city → office), crawlable city links on /directory, claim-listing analytics events, directory + career archives added to marketing nav (dropdown/mobile/footer), 2 broken blog links fixed. All generated from db/et_dental_directory.json via scripts/build_directory.py; npm test clean.
- 2026-07-11 · Kajabi full-fidelity export prep: course_lessons.attachments (applied — per-lesson downloads, e.g. the Modern Dental Assisting 14th-ed chapter PDFs Kajabi lessons carry) + /learn renders signed-URL download buttons from the private course-assets bucket; docs/COWORK-KAJABI-COURSE-EXPORT.md — the complete Chrome/Kajabi export prompt (screenshot every lesson, copy body HTML, download every book chapter/image/file, record every video + quiz + true lesson order, deliver to course-assets/kajabi-export) + the ingest work order (order reconcile, book attachments, image rebuild to match screenshots, Kajabi wording wins, quiz verbatim, Wistia→Mux).
- 2026-07-11 · Course depth pass (owner feedback: site lessons thinner than Kajabi): all 23 authored chapter lessons rewritten to full textbook-chapter depth (16k-27.8k chars each, ~2-3x; complete procedure sequences, materials specifics, errors+fixes, patient scripts, "on the job" sections; quizzes expanded to 10 questions), grounded in researched TSBDE standards (docs/texas-rda-standards.md: approved course+exam = radiology/infection-control/jurisprudence + BLS + trafficking-prevention course); adversarial verify pass fixed a fabricated example name, an unverified regulatory date, link targets; mirrored into rda-program (47 rows). Kajabi API probed live for the verbatim mirror — credential is scope-locked (403 on /v1/courses); unlock path documented in COWORK-REQUEST (enable API course scope or browser export, incl. true lesson ORDER capture).
- 2026-07-11 · Kajabi migration Phase 5 — THE COURSES ARE FULL + purchasable end-to-end: every lesson of both 12-week RDA courses now has real content (weeks 7-10 imported from the kajabi-weeks-7-12 package w/ md5-verified fidelity; weeks 0-7 authored + adversarially verified by a 60+-agent workflow directly into Supabase; weeks 11-12 created in BOTH courses: Ch64/résumé/mock-interview + state-board game plan/mock exam/flashcards/graduation, tool-linked); quizzes converted to the player format; online $397 purchase now routes through buy-product v3 (DEPLOYED: creates account, sets program=career_track + active portal, already-enrolled guard, /learn deep link, magic-link email, buyer automations) — enroll.html online branch + enroll-success "Start my course now →" card; products.online_program row live; rda-program mirror fill.
- 2026-07-10 · Kajabi migration Phase 4: PUBLIC course preview — /courses catalog + per-course curriculum pages (every module/lesson title browsable logged-out, lessons locked 🔒 with enroll/buy CTAs, live stats chips, schema.org Course JSON-LD, sticky buy box, site-facts pricing); backed by new column-safe public.course_catalog view (anon sees structure only — never content/videos/quizzes; underlying RLS verified intact); "Courses" added to marketing nav (desktop+mobile) + sitemap; docs/COWORK-REQUEST-2026-07-10.md (accounts/keys/exports browser legwork prompt).
- 2026-07-10 · Kajabi migration Phase 3: /certificate (printable completion certificate, auto-unlocks at 100%, + "Get my certificate" button in /learn), merge tags in both email crons ({{FIRST_NAME}}/{{UNSUB_URL}} → visible unsubscribe), 12 buyer onboarding drip emails written into sequence_emails (exam_prep/money_plan/survival_planner/career_vault ×3 — net-new, no Kajabi source) + buyer-career-vault sequence & automation rule, leads→automations wiring LIVE (run-automations edge fn + trg_automate_new_lead, verified end-to-end with a test FB lead), CLAUDE.md platform-reality update, docs/claude-code-next-session-prompt.md (the brick-wall handoff).
- 2026-07-10 · Kajabi migration Phase 2: /career-vault sales page (real Square checkout via buy-product; products row career_vault $147/reg $247, live), buy-product v2 deployed (course-aware access emails → /learn?c=slug + purchase automations: subscriber upsert, buyer_<flag> tag, sequence rules — all post-charge best-effort), /admin/emails Email Center (broadcast schedule/unschedule/preview, drip toggles, send log), nav cutover (student "My courses" → /learn, admin nav + Courses/Emails, portal cards → /learn w/ Kajabi fallback link), sitemap + /learn locked→/career-vault.
- 2026-07-10 · Kajabi migration Phase 1 (course platform + email engine, native to the static-HTML stack — NOT the Next.js drop-in): /learn student course player (catalog + outline + YouTube/Mux lessons + quizzes + mark-complete→module_progress), /admin/courses builder (create course + product row, module/lesson editor, direct-to-Mux upload, publish), email crons (broadcast every 15m + drip hourly, Resend, CRON_SECRET-gated), runAutomations engine over public.automations, Mux upload endpoints, scripts/import-emails.mjs (30-day calendar → email_campaigns drafts + Kajabi CSV → subscribers), additive migration (profiles.career_vault + products_admin_all RLS). All additive; nothing sends until Amanda flips campaigns to scheduled. Docs: docs/kajabi-migration-phase1.md.
- 2026-07-07 · Admin leads: phone-first SIMPLE view is now the default — warm greeting + count, big cards for only the people waiting on Amanda (sorted urgent-first), thumb-sized Call/Text/"I handled this" buttons that stamp contact + clear the card. Full inbox (pipeline/filters/tabs/CSV) tucked behind one "See everyone" toggle. Verified in Chromium.
- 2026-07-07 · Day-One Paperwork: /paperwork electronic enrollment (creates account + files signed form + welcome email w/ supply list + payment routing; live-tested end-to-end), /admin/paperwork staff view, enrollment_forms table, enroll-paperwork edge function deployed; Cowork request doc (docs/COWORK-REQUEST-2026-07-07.md); CLAUDE.md automation-first standing priority.

- 2026-07-07 · Master buildout: unified nav shells (admin-nav + student-nav, quicknav retired), persona funnel system (funnel-kit + /go/moms, single-moms, fresh-start, career-change, laid-off + ad copy), seat-aware urgency bar + funnel countdown, dashboard "My prep tools" purchases strip, mini-product entitlement safety (10 profile flags), meta-lead-webhook edge function (deployed, awaiting META_* secrets), buy-product source captured into repo, daily_stats nightly rollup + 180-day retention (pg_cron), KPI first-party traffic section, check:pricing tripwire (caught + fixed dead "paid in full by graduation" copy on /), CLAUDE.md corrections.
- 2026-07-07 · PR #139 merged: July-1 pricing everywhere (assets/tools/admin + marketing + design-reference), tuition planner tool, enroll add-ons, sponsor-a-student program (+ sponsor_profiles table), ask-your-teacher Q&A (+ student_questions table, /admin/questions), calm leads view, 4 branded OG share cards.
- Indexing & trust pack: create missing assets/og-cover.png (161 pages' share image); fix calendar $200→$500 deposit line; classes reserve copy → $500-down truth; city-page plan totals → $3,500; index widget statics; remove TODO leak from FAQ JSON-LD (12 weeks CONFIRMED by Amanda 7/30); online Course schema live-video → self-paced; tour links → /tour site-wide; tap-to-call on tour/apply/waitlist; static prerender of cohort dates on classes/calendar + curriculum overview on /courses + crawlable intros on practice-pro/chairside; graduates 406+ baked (owner-attested); sitemap night-class removed + career-vault deduped; robots sitemap → www; July blog post refreshed to August; site-facts programLength confirmed.
